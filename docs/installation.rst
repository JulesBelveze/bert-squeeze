Installation
===============

To start using **bert-squeeze** you simply need to install the package via pip:

.. code-block::

    pip install bert-squeeze

or directly clone the repository:

.. code-block::

    git clone https://github.com/JulesBelveze/bert-squeeze.git

you are all set, have fun!